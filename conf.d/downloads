#!/bin/bash -ex

dl() {
    [ "$FAB_HTTP_PROXY" ] && PROXY="--proxy $FAB_HTTP_PROXY"
    cd $2; curl -L -f -O $PROXY $1; cd -
}

URL="http://ftp.us.debian.org/debian/pool/main/z/zoneminder/zoneminder_1.29.0+dfsg-1~bpo8+1_amd64.deb"

dl $URL /usr/local/src
